Voici ton nouveau plan d’évolution réajusté. Il intègre un pivot architectural majeur : nous abandonnons la base vectorielle locale sur le device au profit d'une approche 100 % centralisée et cloisonnée avec pgvector dans PostgreSQL (Phase 2).Ce choix est motivé par la réalité de ton code actuel (Python FastAPI + SQLAlchemy) et ton besoin de gérer efficacement le multi-tenant déjà en production, tout en éliminant les risques de saturation des terminaux utilisateurs et la complexité des synchronisations bidirectionnelles.🚀 Plan d'Évolution Technologique & Produit : my-connector (V2)📌 Phase 1 : Consolidation, Sécurisation & Alignement du MVP (0 - 3 mois)Objectif principal : Fermer définitivement l'écart entre la documentation et le code réel (FastAPI/Python). Éliminer le job store en mémoire (ThreadPoolExecutor) pour lever la contrainte du worker unique, implémenter le chiffrement global et connecter le flux Gmail OAuth en souffrance.🛠️ Évolutions Techniques & ArchitectureRéconciliation et Réécriture Documentaire : Mettre à jour architecture.md pour acter la stack réelle : Python/FastAPI + Redis + arq (worker asyncio natif, hautement performant et cohérent avec ton code async actuel). Supprimer toute mention de NestJS/BullMQ.Migration du Job Store (Fin du Single Worker) : Remplacer l'exécuteur en mémoire par un couple Redis + arq avec deux files d'attente étanches :batch-sync-queue : Pour les synchronisations de fond (2x/jour).fasttrack-queue : File à haute priorité pour les rafraîchissements instantanés à la demande.Bénéfice : Autorise le scaling horizontal des workers uvicorn et implémente un retry automatique avec backoff exponentiel (max 5).Livraison du Fast-Track Réel : Implémentation du point d'accès POST /api/projects/{id}/refresh → Retourne un 202 {job_id} immédiat. Le worker traite le delta d'emails depuis le last_processed_email_timestamp et met à jour le triptyque Résumé / Sentiment / Actions. Le frontend fait du polling sur GET /api/jobs/{id}.Fermeture du Gap Gmail OAuth : Brancher enfin gmail_oauth.fetch_emails() dans l' EmailProcessor. Le point d'accès /api/analyze doit basculer automatiquement sur l'API Gmail si les identifiants IMAP ne sont pas configurés.Chiffrement Global au Repos (body_encrypted) : Étendre le chiffrement Fernet (actuellement limité aux identifiants) aux corps des messages et aux textes extraits des pièces jointes (PDF/Excel) dans PostgreSQL via une clé dérivée par Tenant.🎨 Améliorations UX / UIFeedback Fast-Track Ciblé : Implémenter l'état visuel color-fasttrack-active sous forme d'animation pulsée uniquement sur la carte du projet actualisé dans le Project Hub. Le composant lourd de synchronisation globale (SyncGame) reste exclusif à l'onboarding initial.Notifications de Découverte : Introduire le composant color-discovery-banner (Toast non bloquant, auto-dismiss, instance unique) pour signaler la détection de nouveaux emails sans interrompre le travail du Project Manager.📈 Phase 2 : Multi-Tenance, Industrialisation & RAG pgvector (3 - 6 mois)Objectif principal : Formaliser la multi-tenance en production, encadrer la consommation des API (coûts et quotas) et déployer la recherche sémantique centralisée sans ajouter de base vectorielle tierce.🛠️ Évolutions Techniques & ArchitectureIntégration de pgvector dans PostgreSQL : Conformément à l'invariant "PostgreSQL unique pour la persistance primaire", rejeter les solutions type Pinecone ou le stockage vectoriel sur le device. Les embeddings des emails (générés via text-embedding-3-small) sont stockés dans une colonne vector directement dans Postgres.Sécurité de Recherche Intra-Tenant : Verrouiller les requêtes de similarité sémantique par un cloisonnement strict au niveau de la base de données (WHERE tenant_id = :tenant_id), permettant des recherches sémantiques transversales inter-projets en toute sécurité.Régulation des Quotas & Rate-Limiting : Mettre en place un limiteur de type Token-Bucket dans Redis par Tenant pour les requêtes LLM et les appels aux API Gmail/IMAP. Implémenter un mécanisme de Circuit Breaker pour intercepter proprement les erreurs HTTP 429.Optimisation des Coûts : Déployer un cache de réponses des LLM dans Redis basé sur le hash du prompt textuel pour éviter de payer plusieurs fois pour l'analyse d'un contexte inchangé.Correction des Specs : Réviser project-overview.md pour acter la gestion native du Multi-Tenant (JWT, Bcrypt, intégration de la passerelle de paiement CinetPay).🎨 Améliorations UX / UIFiltres de Santé du Dashboard : Ajouter des filtres dynamiques dans le Project Hub basés sur le score de sentiment (color-sentiment-\*), les tags et l'état d'archivage des dossiers.Alertes de Crise Contextuelles : Intégrer un niveau d'alerte color-sentiment-critical (#EF4444) qui se positionne de manière prioritaire dans le flux de notifications de l'utilisateur dès qu'un projet glisse vers un indicateur négatif.Validation Onboarding "1-Click" : Permettre la validation, la fusion ou le renommage immédiat des projets suggérés par le Historical Clustering Worker directement depuis le Validation Board, avec mise à jour optimiste côté UI.🔮 Phase 3 : Intelligence Proactive & Écosystème (6 mois +)Objectif principal : Transformer l'outil en un assistant d'action proactif tout en respectant l'invariant absolu : L'IA ne prend aucune initiative d'envoi de communication sans validation humaine.🛠️ Évolutions Techniques & ArchitectureMoteur de Brouillons de Réponses Locaux : Génération en tâche de fond par le LLM de propositions de réponses aux emails complexes. Ces réponses sont persistées en base avec le statut draft et attendent l'approbation de l'utilisateur. Aucune écriture directe n'est faite sur les API de messagerie sans action de clic.Webhooks de Notification Externes : Développer des connecteurs sortants vers Slack et Microsoft Teams par Tenant pour notifier les équipes lors d'événements majeurs (Deadlines, alertes de sentiment), avec restriction stricte des payloads transmis (métadonnées uniquement, corps de message exclu).🎨 Améliorations UX / UIWidget d'Action Contextuel : Ajouter un volet flottant persistant listant les priorités absolues calculées par l'IA (ex : "Répondre à l'alerte de Sanlam" ou "Confirmer le projet RecoPro").Console de Valorisation du Temps Épargné : Concevoir un écran de statistiques traduisant le volume d'emails traités en indicateur business de productivité (basé sur la règle des 2 minutes).🛠️ Matrice des Risques Révisée & KPIsExplosion des coûts LLM (Clustering historique global) : Le scan initial de boîtes contenant des milliers d'emails (ex: historique massif) peut saturer les budgets.Contingence : Traitement par lots (Batching), usage impératif de gpt-4o-mini pour la phase de pré-clustering et de qualification, et restriction de gpt-4o standard aux analyses fines à la demande.Latences et Erreurs 504 lors du traitement asynchrone : Risque lié à l'ancien exécuteur de tâches en mémoire.Contingence : Résolu en Phase 1 par la mise en place obligatoire de l'architecture découplée FastAPI $\rightarrow$ Redis $\rightarrow$ arq Workers.Qualité du Clustering Initial (KPI Clé) : Le succès de l'onboarding dépend de la pertinence des projets suggérés sur le Validation Board.Mesure : Taux d'acceptation (le pourcentage de projets proposés par l'IA qui sont enregistrés par l'utilisateur sans être modifiés, supprimés ou fusionnés). Cible $\ge$ 85%.
